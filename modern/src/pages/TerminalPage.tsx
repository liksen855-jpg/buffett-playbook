export default function TerminalPage() {
  return (
    <div style={{ padding: 40, textAlign: 'center', color: 'var(--muted)' }}>
      <h1 style={{ fontSize: 24, marginBottom: 12, color: 'var(--fg)' }}>Terminal</h1>
      <p>Porting in progress — see console for typed FMP client demo.</p>
    </div>
  )
}